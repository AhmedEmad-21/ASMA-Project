'use client';

import React from 'react';
import Header from './Header';

export default function ClientHeader() {
  const openModal = () => {};
  const showLogin = () => {};

  return <Header openModal={openModal} showLogin={showLogin} />;
}
